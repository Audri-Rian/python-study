base = input("Qual o tamanho da base? ")
altura = input("QUal o tamanho da altura? ")

base = float(base)
altura = float(altura)

area = base*altura

print(f"A área do quadrado é {area} e o seu doubro é {area*area}")